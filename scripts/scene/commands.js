export function getCommands() {
    return {
        'hero': {
            'right': 'this.mainPerson.right();',
            'left': 'this.mainPerson.left();',
            'up': 'this.mainPerson.up();',
            'down': 'this.mainPerson.down();',
        }
    };
}